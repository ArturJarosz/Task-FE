export const environment = {
    production: true,
    baseUrl: 'BASE_URL',
    auth: {
        auth0: {
            domain: 'AUTH0_DOMAIN',
            clientId: 'AUTH0_CLIENT_ID',
            audience: 'AUTH0_AUDIENCE'
        }
    }
};
