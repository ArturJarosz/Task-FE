export const environment = {
    production: true,
    baseUrl: '/task-backend',
    auth: {
        auth0: {
            domain: 'AUTH0_DOMAIN',
            clientId: 'AUTH0_CLIENT_ID',
            audience: 'AUTH0_AUDIENCE'
        }
    }
};
