/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import {HttpClient, HttpContext, HttpResponse} from '@angular/common/http';
import {Observable} from 'rxjs';
import {filter, map} from 'rxjs/operators';
import {StrictHttpResponse} from '../..250.00/strict-http-response';
import {RequestBuilder} from '../../request-builder';

import {Architect} from '../../models/architect';

export interface CreateArchitect$Params {

    /**
     * New architect data
     */
    body: Architect
}

export function createArchitect(http: HttpClient, rootUrl: string, params: CreateArchitect$Params,
                                context?: HttpContext): Observable<StrictHttpResponse<Architect>> {
    const rb = new RequestBuilder(rootUrl, createArchitect.PATH, 'post');
    if (params) {
        rb.body(params.body, 'application/json');
    }

    return http.request(
        rb.build({responseType: 'json', accept: 'application/json', context})
    )
        .pipe(
            filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
            map((r: HttpResponse<any>) => {
                return r as StrictHttpResponse<Architect>;
            })
        );
}

createArchitect.PATH = '/architects';
