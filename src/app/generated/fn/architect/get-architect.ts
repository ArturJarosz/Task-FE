/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Architect } from '../../models/architect';

export interface GetArchitect$Params {

/**
 * A unique identifier of the architect
 */
  architectId: number;
}

export function getArchitect(http: HttpClient, rootUrl: string, params: GetArchitect$Params, context?: HttpContext): Observable<StrictHttpResponse<Architect>> {
  const rb = new RequestBuilder(rootUrl, getArchitect.PATH, 'get');
  if (params) {
    rb.path('architectId', params.architectId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Architect>;
    })
  );
}

getArchitect.PATH = '/architects/{architectId}';
