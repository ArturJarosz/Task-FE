/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Contractor } from '../../models/contractor';

export interface GetContractor$Params {

/**
 * A unique identifier of the contractor
 */
  contractorId: number;
}

export function getContractor(http: HttpClient, rootUrl: string, params: GetContractor$Params, context?: HttpContext): Observable<StrictHttpResponse<Contractor>> {
  const rb = new RequestBuilder(rootUrl, getContractor.PATH, 'get');
  if (params) {
    rb.path('contractorId', params.contractorId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Contractor>;
    })
  );
}

getContractor.PATH = '/contractors/{contractorId}';
