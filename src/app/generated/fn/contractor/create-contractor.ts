/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Contractor } from '../../models/contractor';

export interface CreateContractor$Params {
  
    /**
     * New contractor data
     */
    body: Contractor
}

export function createContractor(http: HttpClient, rootUrl: string, params: CreateContractor$Params, context?: HttpContext): Observable<StrictHttpResponse<Contractor>> {
  const rb = new RequestBuilder(rootUrl, createContractor.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Contractor>;
    })
  );
}

createContractor.PATH = '/contractors';
