/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Contractors } from '../../models/contractors';

export interface GetContractors$Params {
}

export function getContractors(http: HttpClient, rootUrl: string, params?: GetContractors$Params, context?: HttpContext): Observable<StrictHttpResponse<Contractors>> {
  const rb = new RequestBuilder(rootUrl, getContractors.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Contractors>;
    })
  );
}

getContractors.PATH = '/contractors';
