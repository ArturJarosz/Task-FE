/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Contractor } from '../../models/contractor';

export interface UpdateContractor$Params {

/**
 * A unique identifier of the contractor
 */
  contractorId: number;
  
    /**
     * Data of updated contractor
     */
    body: Contractor
}

export function updateContractor(http: HttpClient, rootUrl: string, params: UpdateContractor$Params, context?: HttpContext): Observable<StrictHttpResponse<Contractor>> {
  const rb = new RequestBuilder(rootUrl, updateContractor.PATH, 'put');
  if (params) {
    rb.path('contractorId', params.contractorId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Contractor>;
    })
  );
}

updateContractor.PATH = '/contractors/{contractorId}';
