/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FinancialReport } from '../../models/financial-report';
import { PeriodType } from '../../models/period-type';

export interface GetFinancialReport$Params {

/**
 * Start date of the report period
 */
  startDate: string;

/**
 * End date of the report period
 */
  endDate: string;

/**
 * Period type for grouping
 */
  periodType: PeriodType;
}

export function getFinancialReport(http: HttpClient, rootUrl: string, params: GetFinancialReport$Params, context?: HttpContext): Observable<StrictHttpResponse<FinancialReport>> {
  const rb = new RequestBuilder(rootUrl, getFinancialReport.PATH, 'get');
  if (params) {
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
    rb.query('periodType', params.periodType, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FinancialReport>;
    })
  );
}

getFinancialReport.PATH = '/financial-data';
