/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SupplyProjectData } from '../../models/supply-project-data';

export interface GetProjectSuppliesData$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;
}

export function getProjectSuppliesData(http: HttpClient, rootUrl: string, params: GetProjectSuppliesData$Params, context?: HttpContext): Observable<StrictHttpResponse<SupplyProjectData>> {
  const rb = new RequestBuilder(rootUrl, getProjectSuppliesData.PATH, 'get');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SupplyProjectData>;
    })
  );
}

getProjectSuppliesData.PATH = '/projects/{projectId}/supplies-data';
