/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Supply } from '../../models/supply';

export interface UpdateSupply$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;

/**
 * A unique identifier of the supply
 */
  supplyId: number;
  
    /**
     * Updated supply data
     */
    body: Supply
}

export function updateSupply(http: HttpClient, rootUrl: string, params: UpdateSupply$Params, context?: HttpContext): Observable<StrictHttpResponse<Supply>> {
  const rb = new RequestBuilder(rootUrl, updateSupply.PATH, 'put');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.path('supplyId', params.supplyId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Supply>;
    })
  );
}

updateSupply.PATH = '/projects/{projectId}/supplies/{supplyId}';
