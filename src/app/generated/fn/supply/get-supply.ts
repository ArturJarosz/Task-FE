/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Supply } from '../../models/supply';

export interface GetSupply$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;

/**
 * A unique identifier of the supply
 */
  supplyId: number;
}

export function getSupply(http: HttpClient, rootUrl: string, params: GetSupply$Params, context?: HttpContext): Observable<StrictHttpResponse<Supply>> {
  const rb = new RequestBuilder(rootUrl, getSupply.PATH, 'get');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.path('supplyId', params.supplyId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Supply>;
    })
  );
}

getSupply.PATH = '/projects/{projectId}/supplies/{supplyId}';
