/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EntityProjectsSummary } from '../../models/entity-projects-summary';

export interface GetClientProjectsSummary$Params {

/**
 * A unique identifier of the client
 */
  clientId: number;
}

export function getClientProjectsSummary(http: HttpClient, rootUrl: string, params: GetClientProjectsSummary$Params, context?: HttpContext): Observable<StrictHttpResponse<EntityProjectsSummary>> {
  const rb = new RequestBuilder(rootUrl, getClientProjectsSummary.PATH, 'get');
  if (params) {
    rb.path('clientId', params.clientId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EntityProjectsSummary>;
    })
  );
}

getClientProjectsSummary.PATH = '/clients/{clientId}/projects';
