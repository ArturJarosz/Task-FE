/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ContractorJob } from '../../models/contractor-job';

export interface UpdateContractorJob$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;

/**
 * A unique identifier of the contractor job
 */
  contractorJobId: number;
  
    /**
     * Updated contractor job data
     */
    body: ContractorJob
}

export function updateContractorJob(http: HttpClient, rootUrl: string, params: UpdateContractorJob$Params, context?: HttpContext): Observable<StrictHttpResponse<ContractorJob>> {
  const rb = new RequestBuilder(rootUrl, updateContractorJob.PATH, 'put');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.path('contractorJobId', params.contractorJobId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ContractorJob>;
    })
  );
}

updateContractorJob.PATH = '/projects/{projectId}/contractor-jobs/{contractorJobId}';
