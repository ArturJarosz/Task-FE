/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Supplier } from '../../models/supplier';

export interface UpdateSupplier$Params {

/**
 * A unique identifier of the supplier
 */
  supplierId: number;
  
    /**
     * Data of updated supplier
     */
    body: Supplier
}

export function updateSupplier(http: HttpClient, rootUrl: string, params: UpdateSupplier$Params, context?: HttpContext): Observable<StrictHttpResponse<Supplier>> {
  const rb = new RequestBuilder(rootUrl, updateSupplier.PATH, 'put');
  if (params) {
    rb.path('supplierId', params.supplierId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Supplier>;
    })
  );
}

updateSupplier.PATH = '/suppliers/{supplierId}';
