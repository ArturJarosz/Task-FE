/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SystemParameter } from '../../models/system-parameter';

export interface UpdateSystemParameter$Params {

/**
 * A unique identifier of the system parameter
 */
  systemParameterId: number;
  
    /**
     * Data of updated system parameter
     */
    body: SystemParameter
}

export function updateSystemParameter(http: HttpClient, rootUrl: string, params: UpdateSystemParameter$Params, context?: HttpContext): Observable<StrictHttpResponse<SystemParameter>> {
  const rb = new RequestBuilder(rootUrl, updateSystemParameter.PATH, 'put');
  if (params) {
    rb.path('systemParameterId', params.systemParameterId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SystemParameter>;
    })
  );
}

updateSystemParameter.PATH = '/system-parameters/{systemParameterId}';
