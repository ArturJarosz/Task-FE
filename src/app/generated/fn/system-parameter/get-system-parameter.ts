/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SystemParameter } from '../../models/system-parameter';

export interface GetSystemParameter$Params {

/**
 * A unique identifier of the system parameter
 */
  systemParameterId: number;
}

export function getSystemParameter(http: HttpClient, rootUrl: string, params: GetSystemParameter$Params, context?: HttpContext): Observable<StrictHttpResponse<SystemParameter>> {
  const rb = new RequestBuilder(rootUrl, getSystemParameter.PATH, 'get');
  if (params) {
    rb.path('systemParameterId', params.systemParameterId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SystemParameter>;
    })
  );
}

getSystemParameter.PATH = '/system-parameters/{systemParameterId}';
