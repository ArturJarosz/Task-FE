/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SupervisionVisits } from '../../models/supervision-visits';

export interface GetSupervisionVisits$Params {

/**
 * A unique identifier of the supervision
 */
  supervisionId: number;
}

export function getSupervisionVisits(http: HttpClient, rootUrl: string, params: GetSupervisionVisits$Params, context?: HttpContext): Observable<StrictHttpResponse<SupervisionVisits>> {
  const rb = new RequestBuilder(rootUrl, getSupervisionVisits.PATH, 'get');
  if (params) {
    rb.path('supervisionId', params.supervisionId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SupervisionVisits>;
    })
  );
}

getSupervisionVisits.PATH = '/supervisions/{supervisionId}/visits';
