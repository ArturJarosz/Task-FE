/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SupervisionVisit } from '../../models/supervision-visit';

export interface UpdateSupervisionVisit$Params {

/**
 * A unique identifier of the supervision
 */
  supervisionId: number;

/**
 * A unique identifier of the supervision visit
 */
  supervisionVisitId: number;
  
    /**
     * Updated supervision visit data
     */
    body: SupervisionVisit
}

export function updateSupervisionVisit(http: HttpClient, rootUrl: string, params: UpdateSupervisionVisit$Params, context?: HttpContext): Observable<StrictHttpResponse<SupervisionVisit>> {
  const rb = new RequestBuilder(rootUrl, updateSupervisionVisit.PATH, 'put');
  if (params) {
    rb.path('supervisionId', params.supervisionId, {"style":"simple","explode":false});
    rb.path('supervisionVisitId', params.supervisionVisitId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SupervisionVisit>;
    })
  );
}

updateSupervisionVisit.PATH = '/supervisions/{supervisionId}/visits/{supervisionVisitId}';
