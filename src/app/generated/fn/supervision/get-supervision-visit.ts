/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SupervisionVisit } from '../../models/supervision-visit';

export interface GetSupervisionVisit$Params {

/**
 * A unique identifier of the supervision
 */
  supervisionId: number;

/**
 * A unique identifier of the supervision visit
 */
  supervisionVisitId: number;
}

export function getSupervisionVisit(http: HttpClient, rootUrl: string, params: GetSupervisionVisit$Params, context?: HttpContext): Observable<StrictHttpResponse<SupervisionVisit>> {
  const rb = new RequestBuilder(rootUrl, getSupervisionVisit.PATH, 'get');
  if (params) {
    rb.path('supervisionId', params.supervisionId, {"style":"simple","explode":false});
    rb.path('supervisionVisitId', params.supervisionVisitId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SupervisionVisit>;
    })
  );
}

getSupervisionVisit.PATH = '/supervisions/{supervisionId}/visits/{supervisionVisitId}';
