/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Supervision } from '../../models/supervision';

export interface CreateSupervision$Params {
  
    /**
     * New supervision data
     */
    body: Supervision
}

export function createSupervision(http: HttpClient, rootUrl: string, params: CreateSupervision$Params, context?: HttpContext): Observable<StrictHttpResponse<Supervision>> {
  const rb = new RequestBuilder(rootUrl, createSupervision.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Supervision>;
    })
  );
}

createSupervision.PATH = '/supervisions';
