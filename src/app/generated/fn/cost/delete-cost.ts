/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface DeleteCost$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;

/**
 * A unique identifier of the cost
 */
  costId: number;
}

export function deleteCost(http: HttpClient, rootUrl: string, params: DeleteCost$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, deleteCost.PATH, 'delete');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.path('costId', params.costId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

deleteCost.PATH = '/projects/{projectId}/costs/{costId}';
