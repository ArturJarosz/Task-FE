/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Costs } from '../../models/costs';

export interface GetCostsForProject$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;
}

export function getCostsForProject(http: HttpClient, rootUrl: string, params: GetCostsForProject$Params, context?: HttpContext): Observable<StrictHttpResponse<Costs>> {
  const rb = new RequestBuilder(rootUrl, getCostsForProject.PATH, 'get');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Costs>;
    })
  );
}

getCostsForProject.PATH = '/projects/{projectId}/costs';
