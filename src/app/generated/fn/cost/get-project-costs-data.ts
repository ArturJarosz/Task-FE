/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CostProjectData } from '../../models/cost-project-data';

export interface GetProjectCostsData$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;
}

export function getProjectCostsData(http: HttpClient, rootUrl: string, params: GetProjectCostsData$Params, context?: HttpContext): Observable<StrictHttpResponse<CostProjectData>> {
  const rb = new RequestBuilder(rootUrl, getProjectCostsData.PATH, 'get');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CostProjectData>;
    })
  );
}

getProjectCostsData.PATH = '/projects/{projectId}/costs-data';
