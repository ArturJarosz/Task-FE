/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Cost } from '../../models/cost';

export interface UpdateCost$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;

/**
 * A unique identifier of the cost
 */
  costId: number;
  
    /**
     * Updated cost data
     */
    body: Cost
}

export function updateCost(http: HttpClient, rootUrl: string, params: UpdateCost$Params, context?: HttpContext): Observable<StrictHttpResponse<Cost>> {
  const rb = new RequestBuilder(rootUrl, updateCost.PATH, 'put');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.path('costId', params.costId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Cost>;
    })
  );
}

updateCost.PATH = '/projects/{projectId}/costs/{costId}';
