/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Cost } from '../../models/cost';

export interface GetCost$Params {

/**
 * A unique identifier of the cost
 */
  costId: number;
}

export function getCost(http: HttpClient, rootUrl: string, params: GetCost$Params, context?: HttpContext): Observable<StrictHttpResponse<Cost>> {
  const rb = new RequestBuilder(rootUrl, getCost.PATH, 'get');
  if (params) {
    rb.path('costId', params.costId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Cost>;
    })
  );
}

getCost.PATH = '/projects/costs/{costId}';
