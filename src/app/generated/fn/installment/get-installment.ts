/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Installment } from '../../models/installment';

export interface GetInstallment$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;

/**
 * A unique identifier of the installment
 */
  installmentId: number;
}

export function getInstallment(http: HttpClient, rootUrl: string, params: GetInstallment$Params, context?: HttpContext): Observable<StrictHttpResponse<Installment>> {
  const rb = new RequestBuilder(rootUrl, getInstallment.PATH, 'get');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.path('installmentId', params.installmentId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Installment>;
    })
  );
}

getInstallment.PATH = '/projects/{projectId}/installments/{installmentId}';
