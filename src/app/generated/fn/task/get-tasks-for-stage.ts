/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Tasks } from '../../models/tasks';

export interface GetTasksForStage$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;

/**
 * A unique identifier of the stage
 */
  stageId: number;
}

export function getTasksForStage(http: HttpClient, rootUrl: string, params: GetTasksForStage$Params, context?: HttpContext): Observable<StrictHttpResponse<Tasks>> {
  const rb = new RequestBuilder(rootUrl, getTasksForStage.PATH, 'get');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.path('stageId', params.stageId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Tasks>;
    })
  );
}

getTasksForStage.PATH = '/projects/{projectId}/stages/{stageId}/tasks';
