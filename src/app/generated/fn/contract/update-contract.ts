/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Contract } from '../../models/contract';

export interface UpdateContract$Params {

/**
 * A unique identifier of the contract
 */
  contractId: number;
  
    /**
     * Data of contract to update
     */
    body: Contract
}

export function updateContract(http: HttpClient, rootUrl: string, params: UpdateContract$Params, context?: HttpContext): Observable<StrictHttpResponse<Contract>> {
  const rb = new RequestBuilder(rootUrl, updateContract.PATH, 'put');
  if (params) {
    rb.path('contractId', params.contractId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Contract>;
    })
  );
}

updateContract.PATH = '/contracts/{contractId}';
