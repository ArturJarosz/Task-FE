/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Contract } from '../../models/contract';

export interface ChangeStatus$Params {

/**
 * A unique identifier of the contract
 */
  contractId: number;
  
    /**
     * Data of contract to complete
     */
    body: Contract
}

export function changeStatus(http: HttpClient, rootUrl: string, params: ChangeStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<Contract>> {
  const rb = new RequestBuilder(rootUrl, changeStatus.PATH, 'post');
  if (params) {
    rb.path('contractId', params.contractId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Contract>;
    })
  );
}

changeStatus.PATH = '/contracts/{contractId}/status';
