/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Stage } from '../../models/stage';

export interface CreateStage$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;
  
    /**
     * New stage data
     */
    body: Stage
}

export function createStage(http: HttpClient, rootUrl: string, params: CreateStage$Params, context?: HttpContext): Observable<StrictHttpResponse<Stage>> {
  const rb = new RequestBuilder(rootUrl, createStage.PATH, 'post');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Stage>;
    })
  );
}

createStage.PATH = '/projects/{projectId}/stages';
