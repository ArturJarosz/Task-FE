/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Stage } from '../../models/stage';

export interface UpdateStage$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;

/**
 * A unique identifier of the stage
 */
  stageId: number;
  
    /**
     * Updated stage data
     */
    body: Stage
}

export function updateStage(http: HttpClient, rootUrl: string, params: UpdateStage$Params, context?: HttpContext): Observable<StrictHttpResponse<Stage>> {
  const rb = new RequestBuilder(rootUrl, updateStage.PATH, 'put');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.path('stageId', params.stageId, {"style":"simple","explode":false});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Stage>;
    })
  );
}

updateStage.PATH = '/projects/{projectId}/stages/{stageId}';
