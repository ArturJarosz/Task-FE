/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Stage } from '../../models/stage';

export interface ReopenStage$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;

/**
 * A unique identifier of the stage
 */
  stageId: number;
}

export function reopenStage(http: HttpClient, rootUrl: string, params: ReopenStage$Params, context?: HttpContext): Observable<StrictHttpResponse<Stage>> {
  const rb = new RequestBuilder(rootUrl, reopenStage.PATH, 'post');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
    rb.path('stageId', params.stageId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Stage>;
    })
  );
}

reopenStage.PATH = '/projects/{projectId}/stages/{stageId}/reopen';
