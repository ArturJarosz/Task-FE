/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Stages } from '../../models/stages';

export interface GetStagesForProject$Params {

/**
 * A unique identifier of the project
 */
  projectId: number;
}

export function getStagesForProject(http: HttpClient, rootUrl: string, params: GetStagesForProject$Params, context?: HttpContext): Observable<StrictHttpResponse<Stages>> {
  const rb = new RequestBuilder(rootUrl, getStagesForProject.PATH, 'get');
  if (params) {
    rb.path('projectId', params.projectId, {"style":"simple","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Stages>;
    })
  );
}

getStagesForProject.PATH = '/projects/{projectId}/stages';
